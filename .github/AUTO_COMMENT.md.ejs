---
when:
  event:
    $in:
      - issues
      - pull_requests
---

Hi <%= payload.issue.user.login %>,

Thanks for submitting this <%= event === "issues" ? "issue" : "merge request" %>.

<% if(event === "merge_requests")  { %>
  [Docs](http://example.readthedocs.org/en/<%- merge_request.source_branch %>/)
<% } %>
